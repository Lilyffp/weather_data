This repository contains an analysis comparing average rainfall through the year for Melbourne and Oxford

To run this analysis use the following command:

```
Rscript analysis/make-plot.R
```

The input data is in `data`,
the command to combine data and the combined average rainfall is in 'merged-data'  and the results are in `out`.
